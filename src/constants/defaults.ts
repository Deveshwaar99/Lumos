import type { AppSettings } from '../models/types';

export const DEFAULT_SETTINGS: AppSettings = {
  baseCurrency: 'USD',
  decimalPlaces: 2,
  currencySymbol: '$',
  lastBackupAt: null,
  username: '',
};
